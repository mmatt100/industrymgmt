services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      SQLITE_DB_PATH: /app/data/data.sqlite
      NODE_ENV: production
    volumes:
      - data:/app/data

volumes:
  data:
